[pytest]
markers =
    gpu: tests that require CUDA or GPU runtime
    models: tests that require local model files or heavyweight inference dependencies
    external: tests that require external network services or live third-party APIs
