# Local development defaults. Production container builds do not rely on this file;
# when env vars are omitted in production, the frontend defaults to the Nginx same-origin proxy.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
VITE_WS_BASE_URL=ws://127.0.0.1:8000
VITE_LIVE2D_MODEL_PATH=/live2d/haru/haru_greeter_t03.model3.json
VITE_HEARTBEAT_MS=15000
VITE_RECONNECT_BASE_MS=1200
